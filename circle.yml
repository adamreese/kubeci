machine:
  environment:
    KUBECTL: ~/.kube/kubectl
  services:
    - docker
dependencies:
  pre:
    - go env
    - docker info
    - ./scripts/ensure-kubectl.sh
test:
  pre:
    - ./scripts/local-cluster.sh up
