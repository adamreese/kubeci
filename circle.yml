machine:
  environment:
    KUBECTL: ${HOME}/.kube/kubectl
  services:
    - docker
dependencies:
  pre:
    - go env
    - docker info
  override:
    - ./scripts/ensure-kubectl.sh
test:
  pre:
    - ./scripts/local-cluster.sh up
